server:
  port: 8080
eureka:
  instance:
    appname: center
    hostname: localhost
  #停止向其他注册中心注册
  client:
    register-with-eureka: false
    fetch-registry: false
  #关闭自我保护
#  server:
#    enable-self-preservation: false